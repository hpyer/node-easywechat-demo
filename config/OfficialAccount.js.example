const { defineOfficialAccountConfig } = require('node-easywechat');

module.exports = defineOfficialAccountConfig({
  // 微信公众号的 app key
  app_id: '',
  // 微信公众号的 app secret
  secret: '',
  // 微信公众号的 token
  token: '',
  // EncodingAESKey
  aes_key: '',

  // 网页授权认证
  oauth: {
    // 网页授权类型
    scope: 'snsapi_userinfo',
    // 网页授权回调地址，完整的URL
    callback: 'http://example.com/wxlogin/callback'
  },
});
